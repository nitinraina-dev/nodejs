argv is an array of all command-line arguments.

argv[0] is the path to the Node.js executable.

argv[1] is the path to the script file being executed.

argv[2] is the first real argument you provide when running the script.
